# バックログ（優先順）

毎セッション読む必要はない参照資料なので `CLAUDE.md` から分けている。
着手前に `.claude/rules/10-guardrails.md` の設計制約に照らすこと。

## 運用の決定（2026-08-16）

コードではなく運用の取り決め。v3.0 リリース後の点検で決めた。
**期日のあるものは「28日棚卸し（〜2026-09-05）」に集約する。**

- **28日棚卸しの基準は「この項目を対象にした【来週の一点】を、今後打つ可能性がまだあるか」。**
  「見て行動を変えたか」ではない。**変えられていないという事実の計測にも意味がある**というのが
  オーナーの判断で、停滞の観測は週次ループへの正当な入力だから。ただし成り立つのは
  その項目が一点の候補である間だけで、候補ですらなくなった項目の停滞データは
  どの意思決定にも入らない。現時点の判定候補: サウナ / 休肝日 / 夕サプリ。
- **朝の入力を1回だけ実測する（任意）。** 制約③の「20秒」は 2026-08-17 に撤廃したので、
  合否の基準としては不要になった。それでも測る価値があるとすれば、削減の実利を数字で見たいときだけ。
- **週の目標（`targets`）を1度棚卸しする。** 現在は全項目が出荷時の既定値のまま。
  項目ごとに「追う目標」か「基準線」かを決める。**目標は週ごとに保存されず全期間で1つなので、
  変えると過去週の表示にも遡って効く。** 変えた日と内容を週報チャットに1行残すこと。
- **週報の冒頭に「前週の一点: 実行/未実行・観察したこと」を1行書く。**
  これが介入台帳になる。現状「いつ何を変えたか」はどこにも構造的に残らず、
  90日・1年後に before/after を見られない。アプリ側の変更はゼロ。
- **晩の記録（晩の気分・夕食・晩サプリ）は夕食直後に寄せ、就寝前には開かない。**
  「就寝前にスマホを持つ理由を増やさない」という一点の原則と、v3.0 で足した晩枠が
  衝突するため。運用で解決し、アプリは変えない。
- **項目追加の規律は設けない（明示的な決定）。** 実測ゲートも項目数の上限も置かない。
  したがって**項目の純増に対する歯止めは現状どこにも無い**。棚卸しがその役割を負う。
- **昼寝（`napMin`、2026-08-18）は他項目の説明変数として置く。** 業務時間・仕事の重さと同じ位置づけで、
  **昼寝自体を対象にした【来週の一点】は書かない**（オーナー確認済み）。棚卸しで「一点を書けないから外す」
  と判断しないこと。役割は眠れた感・朝の気分・晩の気分を説明することで、目標化は意図的に見送っている。
- **昼寝する日は疲労度ではなく曜日で決める（N-of-1、2026-08-18 オーナー決定）。** アプリの変更はゼロ。
  疲れた日ほど昼寝するという交絡（confounding by indication）があるため、そのまま記録すると
  効果が実在しても φ は 0 か負に寄る。この運用が続いていない期間の φ は、効果の推定として読まないこと。
  相関ヒントの `foot` はこの交絡を注記として出しているが、注記は交絡を解かない。
- **晩の気分は調子に関わらず毎晩入れる（2026-08-18 オーナー決定）。曜日決め打ちとセットで初めて
  「昼寝 × その日の晩の気分」が読める。** 昼寝の効果側に届く対はこの1本だけだが、そこには
  **2つの歪みが同時に乗っている**: 曝露側の交絡（疲れた日ほど昼寝する）と、分母側の自己選択
  （晩の枠は別の機会にアプリを開かないと埋まらず、開くかどうか自体が調子と相関する）。
  曜日決め打ちは前者にしか効かない。**片方だけを直しても φ は読めない**（guardrail-reviewer の指摘）。
- **相関ヒントの行数に上限は無い（28本、2026-08-18 時点）。** `strength()` は並び順を決めるだけで、
  下側限界が 0 の行も表示される。「ノイズを最上位に出さない」防御はあるが「ノイズを出さない」防御は
  14日フィルタだけ。**歯止めを置くかは次に行を足すときに判断する**（オーナー判断で今回は保留）。

### 失敗条件（これが観測されたら設計を疑う）

- 入力の7日以上の断絶が2回 → 摩擦過多
- 週報を4週連続で貼らない → 計測の自己目的化
- **入力が完璧なまま、達成率がどの項目も12週間動かない** → 計測だけが回り環境設計が行われていない

## P1

- **PCへの同期が毎日届いていない。** 2026-08-08〜15 の8日間で PC に残っているスナップショットは
  3日ぶん（08-10 / 14 / 15）だけ。受信ログには 08-13 の
  「起動できません: 環境変数 AUBADE_SYNC_TOKEN が未設定か 32文字未満です」が残っている。
  端末が失われたらその期間の記録は消える。受信タスクの常駐とトークン設定を確認すること
  （手順は `app-hub` の README と `tools/install-receiver-task.ps1` が正本）。
  **90日・1年のデータ活用は、この経路が毎日動くことに依存する。**
- **実機での長期永続性の観察。** iOS / WebKit のサイトデータ削除ポリシーと、ホーム画面PWA容器の挙動。
  最終バックアップ日の表示とファイル書き出しは v2.4 で入れた。残るのは**実際に消えるかの観察**。
- **iOS 実機でのオフライン起動の確認。** Service Worker は導入済みで Chromium では確認できているが、
  iOS Safari とホーム画面PWAの挙動は未確認。機内モードで開けるかを実機で見る。
- **遡及入力の上限（現在7日）の妥当性レビュー。**

## P2

- **週報のアプリ内AI生成オプション。** オーナー自身の Anthropic API キーを用いたブラウザ直呼び。
  CORS 許可ヘッダ（`anthropic-dangerous-direct-browser-access`）の現行仕様を docs.claude.com で要確認。
  キーの保存場所（`localStorage` の是非）はオーナーと要相談。既定はオフ。
  **CSP の `connect-src` 緩和とセットで設計し、緩和範囲を必要最小限のホストに限ること。**
- ~~**月間ビュー。**~~ 2026-08-16 に実装（推移タブの先頭・1項目×カレンダー）。
  設計の判断は `CLAUDE.md` の不変条件にある。

## P3

- **Web Push。** iOS のホーム画面PWAで技術的には可能だが、現状は OS のリマインダーで充足しており優先度は低い。
- **項目タイプの拡張**（数値項目の汎用化）。追加時も1項目1〜2タップの制約を守ること。

## やらないこと

`.claude/rules/10-guardrails.md` にまとめてある。
