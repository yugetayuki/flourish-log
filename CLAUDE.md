# Aubade — 開発引き継ぎドキュメント

個人用の朝の行動計測PWA。オーナーの幸福(flourishing)フレームワーク実装の一部として、
Claude.ai チャット上で要件定義〜v2.0まで開発された。本書は Claude Code への引き継ぎ資料。

- 最終更新: 2026-08-21 / 現行バージョン: **v4.28**(`index.html` 内 eyebrow 表記と一致させること)
- テスト: `npm install && npm test`(vitest) と `npm run smoke`(実ブラウザ)。
  本数は `pwa.test.js` と `tools/smoke.mjs` が正なのでここには書かない

### 名前について(v2.3で改称)

表示名は **Aubade**(オーバード / 夜明けの歌)。`<title>` / `<h1>` / `apple-mobile-web-app-title` の
3箇所にあり、テスト「表示名 Aubade が…一致する」が食い違いを検出する。

**識別子は旧名 `flourish-log` のまま据え置く。** 改称に合わせて変えてはいけない:
- `localStorage` キー `flourish-log-v2` — 変えると既存の記録が読めなくなる(退避キーの接頭辞も同様)
- リポジトリ名と公開URL — 変えるとホーム画面に追加済みのPWAが切れる
- `window.__flourish` テストフック / `console.error("[flourish] …")` の接頭辞

旧称は「繁栄ログ (Flourish Log)」。v2.2 までのコミットとチャット履歴ではこの名前で出てくる。

### 規約はすべてリポジトリ内にある

ローカルの `~/.claude/` はそのマシンにしか存在せず、モバイルやクラウド環境のセッションには届かない。
どこから開いても同じ挙動になるよう、規約は全てここに置いてある。

| ファイル | 役割 |
|---|---|
| `CLAUDE.md`（本書） | このプロジェクトの事実と不変条件。毎セッション読み込まれる |
| `.claude/rules/00-common.md` | 全プロジェクト共通の開発規約。**3リポジトリで同一内容**。毎セッション読み込まれる |
| `.claude/rules/10-guardrails.md` | 死守すべき設計制約。受け入れ基準。毎セッション読み込まれる |
| `.claude/skills/release/SKILL.md` | リリース手順。`/release` で呼ぶ |
| `.claude/agents/guardrail-reviewer.md` | 設計制約の審査役サブエージェント |
| `.claude/settings.json` | 権限（allow / ask / deny）と effortLevel |
| `BACKLOG.md` | 参照資料。毎セッションは読まない |

---

## 1. プロダクトの役割定義(最重要・変更禁止)

このアプリは **「行動を起こさせる装置」ではなく「短時間の日次計測+週次フィードバックの装置」**。
行動変容の主役は環境設計(アプリの外)にあり、アプリは (a) 未定着行動の定着期の足場、
(b) 週次フィードバックループの計測層、の2役に限定する。

**死守すべき設計制約7項目と「恒久的にやらないこと」は `.claude/rules/10-guardrails.md` にある。**
このファイルと同じく毎セッション読み込まれる。受け入れ基準はそちらが正。

**次のいずれかに当たる変更は、コミット前に `guardrail-reviewer` サブエージェントへ出す。**
「判断に迷ったら」ではなく、条件で決める(制約5に触れる変更は自然な改善に見えるため、迷い自体が起きない):

- 記録項目を足す・消す・改名する
- 画面に出る文言を変える(ラベル・注記・週報テキストの指示文を含む)
- 週タブ・目標・相関ヒント・月間ビューの**集計や表示の対象**を変える
- 達成判定(`achieved` / `studyDone` / `th`)に触れる
- `/release` を実行する(スキルの手順2。最後の関門)

審査役はコードの良し悪しを見ない。制約への抵触だけを見る。
**指摘が出たら実装を自分で変えず、オーナーへ上げる。** 制約はオーナーとの合意事項なので、
抵触の解消は設計判断であって実装の調整ではない。

---

## 2. 現状

- **形態**: 依存ゼロのPWA。出荷物は `index.html` と `sw.js` の2本だけ。
  vanilla JS + インラインCSS + インラインSVGチャート。フレームワーク・CDN・ビルド工程なし
  (オフライン耐性と検証容易性のための意図的選択)。`sw.js` は別ファイルでしか登録できない仕様上の分割で、
  中身は index.html のキャッシュだけを見る(v2.4 までは単一ファイルだった)。
- **保存**: `localStorage`、キー `flourish-log-v2`、即時書き込み。
- **ホスティング**: GitHub Pages。リポジトリ https://github.com/yugetayuki/flourish-log (Public) /
  公開URL https://yugetayuki.github.io/flourish-log/ 。`main` に push すると数分で反映。
- **通知**: アプリ自体は通知を持たない。OS標準のリマインダーを朝の決まった時刻に1本置く運用に委ねる
  (設定済み。具体的な時刻はオーナーの生活時間に合わせてある)。アプリ側に通知・催促を追加しないこと(設計制約5)。
- **週次AIレビュー**: アプリ内生成ではない。週報タブ「週報用データをコピー」→ オーナーが
  Claude.ai の該当プロジェクトチャットに貼り付け → Claudeがレビューを返す運用。
  出力形式の契約: 【事実】3行以内 / 【解釈】2行以内(推測と明示) / 【来週の一点】1つだけ。
  責めない・説教しない・淡々と。環境設計(摩擦の調整)の観点で提案する。
- **タブ**: 記録 / タイマー / 週 / 推移 / 週報 / 設定 の6つ。中身は `TABS` と各 `viewXxx()` を読めば分かる。
  遡及入力は過去7日まで、先取り入力は**明日まで**(v4.4)。就寝時刻と前日の行動は夜のうちに
  確定しているので、翌朝まで抱えて思い出すより、その場で入れた方が記録が正確になる。
  明日ぶんを開いているときは同じカードが指す時点が変わるため、見出しを
  「今夜 / 明日の朝 / 今日」に切り替える(「昨夜」のままだと何を入れているか分からなくなる)。
  未来日は `weekStats` が集計から外すので、達成に数えられるのは日付が変わってから。
  - **記録タブのカードは入力頻度順に並べる(v4.28、オーナー判断 2026-08-21)。**
    昨夜 → 昨日 → 今朝 → 今日の…(水分 / 昼寝 / 食事 / サプリ / 気分)。
    **v4.24 の「上から下へ時間が進む順」(昨日→昨夜→今朝)を置き換えたもの。**
    就寝時刻へ着くまでに前日の行を全部またぐ必要があった。**頻度の出所はオーナーの申告**で、
    アプリは項目ごとの入力回数を持っていない(実測ではない。制約3の「20秒」を撤廃したのと同じ形なので、
    測っていない数字を判定基準に据えない。頻度の実感が変わったら並びも再検討してよい)。
    **時系列は崩れている**(昨夜=前日の夜 → 昨日=前日の昼 で半日戻る)。承知のうえで、
    毎朝そこへ到達する距離を取った。v4.24 が解こうとした「どの時点を入れているか分からない」は
    カード見出しの日付語が担保する。**時系列へ戻すのは蒸し返しなので、オーナーに確認してから。**
    v4.23 まで「昨日」は3枚目にあり、上から眺めると3枚目で1日戻っていた(v4.24 の動機)。
    **これは表示の順序だけの変更で、`entries` の持ち方は動かしていない**
    (どのフィールドがどの時点を指すかは下の「entriesの意味論」のまま)。
    並びは `pwa.test.js` の見出し完全一致テストが縛っている。**`arrayContaining` へ緩めないこと** —
    カード順に対する機械的なガードはそこだけで、緩めると順序が黙って戻せるようになる。
  - **行のヒントに日付語を書かない(v4.24)。** 時点の出所はカード見出しの
    `(ahead?'今日':'昨日')` に一本化してある。行にも「昨日の」「前日に」を重ねると、
    明日ぶんを開いて見出しが「今日」に変わったときに行だけ食い違う(実際にそうなっていた)。
    **相関ヒントのラベル(「前日に酒を飲んだ」等)と週報テキストの凡例は別**で、
    あちらは日付語が唯一の時点情報なので落とさない。
  相関ヒントは28日ロック。**数えるのは「相関に使う項目を1つ以上持つ、今日以前の日付キー」**で、
  `entries` の日付キーの数ではない(メモだけ・体重だけの日と、先取り入力の未来日を算入しない)。
  分母の印は `mk` / `mkNext` が値を読んだついでに付ける(`corrDays`)。**項目名の一覧を別に持たないこと** —
  持つと対を足したときに片方だけ腐り、材料の無い日を数えたまま気づけない。
  そのため `defs` はロック中も建てる(建てないと `n` が分からない)。ロック中も全日付を舐めるので、
  `mk` / `mkNext` は `S.entries[k]||{}` で null のエントリに倒すこと(取り込みJSONは素通し)。
  設定タブの「エントリ ◯件」はこれとは別の数で、メモだけの日も未来日も1件に数える。
  推移の表示期間は28日/90日で切替(`trendDays`、保存しない)。
  推移タブの先頭に月間ビュー(1項目×カレンダー。`monItem` / `monOff` も保存しない)。
- **PCへの同期(v2.8・任意)**: 設定タブで送信先URLとトークンを入れると、保存のたびに
  数秒まとめてオーナーのPCへ全履歴のスナップショットを POST する。既定は空欄=何も送らない。
  iOS の PWA はバックグラウンドで動けないので、**送信はアプリを開いている間だけ**。
  差分もリトライ待ち行列も持たないのは、毎回が全履歴なので**次に成功した1回で追いつく**ため。
  受信側(`tailscale serve` + 受信スクリプト)と、届いた先での変換・台帳への取り込みは
  別リポジトリ `app-hub` にある(`tools/aubade-receiver.mjs` / `tools/aubade-to-hub.mjs`)。
  **設定手順はそちらの README が正本**。ここには置かない。
  **記録項目を足す・改名するときは、app-hub 側の許可リスト(`tools/aubade-to-hub.mjs`)を先に直すこと**
  (`CORE` の項目でも、`wakeMin` のように `enabled` だけを持つ計測項目でも同じ)。
  **反映してから受信タスク `AubadeReceiver` を再起動する。** node がモジュールを読み直さないので、
  `git pull` だけでは古い許可リストのまま動き続ける。改名しても旧名は消さない
  (それ以前に書き出したバックアップを後から変換する経路が生きる)。
  拒否リストではなく許可リストなので、足すまで新項目は台帳に載らない。落ちたことは受信PCのログと
  200応答の本文にしか出ず、`syncPush()` は本文を読まないので端末には「同期しました」と緑で出る
  (気づく経路が無い)。app-hub 側に突き合わせのガードテストを置いてある。

---

## 3. アーキテクチャ

### 状態管理パターン
- 単一状態オブジェクト `S`(モジュール変数)。
- 変更 → `save()`(localStorage即時書き込み+ヘッダーに「✓ 保存済み」フラッシュ)→ `render()`(該当タブを innerHTML 全再描画)。
- イベントは `document` への委譲1本。`data-*` 属性でルーティング:
  `data-nav`(タブ) / `data-dn`(日付±) / `data-f`+`data-v`(セグメント・ペアボタン。同値タップで未入力に戻る=トグル) /
  `data-tgt`+`data-d`(目標ステッパー) / `data-tog`(表示トグル) / `data-del`(カスタム削除) / `data-action`(その他) /
  `change` は `#wv`(体重数値)、`select[data-f]`(分の実値)、`[data-th]`(達成ライン)、`[data-mon]`(月間ビューの項目)。
  **`data-f` は seg/pairBtn のボタンと select の両方が持つ。** クリック側は `[data-f][data-v]` で絞る
  (絞らないと select をタップしただけで値が入る)。select は空欄「—」を選ぶとキー削除で未入力へ戻り、
  これが seg の同値タップに相当する。
- 再描画で入力フォーカスが飛ぶため、自由入力は `change` イベント(blur/確定時)でのみ処理する方針。踏襲すること。

### データモデル(localStorage `flourish-log-v2`、schema v22)

形は `defaultData()` を読めば分かる。コードから読み取れない意味論だけを書く。

- `th` = 達成ライン。**向きは項目ごとに違う**。`bedtimeMin`(分)と `youtubeMin`(分)は
  **この値以下で達成**、`steps`(1..3、インデックス)だけは**このインデックス以上で達成**。
  steps で最下段(0)を選べるようにすると全ての日が達成になるので、選択肢から外してある。
  **入力が15分刻みでも達成ラインの選択肢は30分刻みのまま**(就寝 22:00〜24:30、YouTube 30分/1h/2h)。
  達成ラインの就寝の範囲は 2026-08-16 に 22:00 まで下へ広げた(オーナーが22:00就寝を目標にしたため)。
  **広げたのは範囲であって刻みではない。** 入力側の天井を外したのと同じ理屈で、
  目標が選択肢の外にあると表現できないのが問題だった。刻みを15分にするのは別の判断で、混ぜないこと。
  15分単位で締め上げられる状態は完璧主義に対する逆向きのラチェットになるため(設計制約1の趣旨)。
  刻みを揃えたくなったらオーナーに相談すること。
  - **入力側(`BEDTIME_MIN`)の範囲とは別物。** 入力は 2026-08-20 に 20:00〜26:00 へ広げたが、
    達成ラインは 22:00〜24:30 のまま揃えていない。**揃え忘れではない。** 新設の 20:00〜21:45 は
    `th` の最小(1320 = 22:00)より必ず小さく、`achieved` が `e.bedtimeMin <= th` なので
    **選んで未達になる経路が存在しない**。広げる必要が無い。
    **`th` を 22:00 より下へ広げないこと** — そちらは未達が増える向きで、上の
    「逆向きのラチェット」に当たる。`pwa.test.js` の「達成ラインは30分刻みのまま、22:00 まで選べる」が縛っている。
  - **端に「20時以前」「26時以降」のような開区間を置かない(2026-08-20 に再検討して不採用)。**
    `bedtimeMin` は分の実値なので、開区間には実時刻でない値を1つ決める必要があり、
    睡眠時間・推移・帯グラフ・週報・台帳がそれを本物の時刻として扱う。
    **同じことを一度やって捨てている** — v9 以前の「24:30以降」で、実際に何時に寝たかが
    記録から消えた(`WATER_ML` / `NAP_MIN` の天井のコメントが今もこの前例を引いている)。
    範囲が足りないと感じたら、開区間ではなく**実値の範囲を広げる**こと。
- **entriesの意味論(重要)**: キーの日付=「記録した朝」。フィールドが指す時点は
  前夜(bedtimeMin, ashwagandha)/ 当朝(wakeMin, sleepFeel, coffee, creatine, weight, weightVal)/
  前日(youtubeMin, stepsCount, workMin, workLoad, gym, studyMin, meditationMin, sauna, bath, mouth, protein, steps, sober, eatout, companion, カスタム)/
  **当日(`waterMl`、`napMin` と `PERDAY` の各 `parts` と `MOOD.parts`)**。
  **カスタム項目は必ず前日を指す**(「昨日」カードに描画される)。当朝・前夜の行動を足すときは `CORE` 側に置くこと。
- **`PERDAY` の項目だけが当日を指す(v2.6 で食事、v2.7 で整腸剤・サプリ)。** 朝にまとめて入力する
  他の項目と違い、食べた/飲んだその場で1タップする運用なので、記録した朝ではなくその日自身を指す。
  週タブと目標では3行に増やさず、`achieved()` が1日ぶん(`meal` / `supp`)に畳む
  (**記録した回がすべて達成なら達成**、1つも記録がなければ未入力)。
  記録しなかった回を分母に入れない理由は設計制約2(欠損は失敗ではない)。
  3行に増やさない理由は、1日あたりの未達の面積が3倍になり設計制約1・2の趣旨と衝突するため。
  **同種の項目を足すときは `PERDAY` に1要素足すだけで済むようにしてある**
  (`CORE` / 表示トグル / CSV列 / 記録タブのカードはすべてここから導出する)。
- **`sober`(休肝日)は「飲まなかった」が `true`。** 「飲んだ」を達成側に置くと
  週タブの「4/4」が飲んだ日数に読めてしまうため、達成の向きを他の項目と揃えてある。
  記録タブのボタンだけ「✓ 飲まなかった / ✗ 飲んだ」とラベルを差し替えている(`pairBtn` の第3引数)。
- 値の欠損はキー自体を削除して表現(`null` を書き込まない)。全フィールド空になったらその日付キーごと削除。

### 不変条件・約束事
- **`migrate()` が後方互換の要。** スキーマ変更時は必ず `migrate()` を拡張し `version` を上げる。
  既存ユーザーデータ(JSON取り込み経由の旧データ含む)が壊れないことをテストで保証すること。
- **`window.__flourish` テストフックを削除しない。** `pwa.test.js` が依存している。
  **一覧の真実は `index.html` の `window.__flourish` リテラル**で、ここには書き写さない
  (書き写すと増えたときに必ず腐る)。削除・改名するときは同時に `pwa.test.js` を見ること。
- 達成判定は `achieved(data, entry, id)` に一元化。週集計は `weekStats()`。φ係数は `phi()`(分母0は0を返す)。
- 週の起点は月曜(`monIdx`)。日付キーはローカルタイム `YYYY-MM-DD`。
- **保存データを読めなかったとき、原本を消さない(v2.1)。** `load()` は JSON.parse に失敗したら
  原本を `flourish-log-v2-broken-<YYYY-MM-DD>` へ退避してから初期状態で起動し、`#banner` に警告を出す。
  退避にも失敗した場合は `saveBlocked` を立てて `save()` を止める(初期状態での上書き = 復旧不能を防ぐため)。
  退避データは設定タブ →「データ」から取り出して取り込み欄に戻せる。この経路を壊さないこと。
- **`copyText(text, done)` は非同期。** `navigator.clipboard.writeText` は Promise を返すため、
  同期の戻り値で成否を判断しない。呼び出し側は失敗時の文言を先に描画し、成功したときだけ差し替える
  (成功表示を既定にすると、拒否された場合に「コピーしました」と嘘をつく)。
- **CSV は `csvCell()` を通す(RFC4180)。** カスタム項目のラベルは自由入力なので、
  カンマ・引用符・改行で列がずれる。ヘッダー・データ行の両方に適用すること。
- **HTML属性への値の埋め込みは必ず `esc()` を通す(v2.2)。** `data-f` / `data-tgt` / `data-del` /
  `data-key` にはカスタム項目のIDが入り、IDは取り込みJSON経由で任意の文字列になりうる。
  `esc()` は `& < > " '` を変換するので、二重引用符で囲んだ属性なら安全。
  `dataset` 経由で読むと元の文字列に戻るため、エスケープしても機能は変わらない。
- **`migrate()` が型の境界(v2.2)。** JSON取り込みが唯一の外部入力経路なので、ここで
  `isPlainObject()` / `normCustom()` により型を揃える。これを通った後のコードは型を再検査しない。
  **`entries` の中身は原則として素通し**(日付キーごとに舐めると取り込みが重くなるため)。
  例外は2つ: v7 の歩数の読み替えと、v9 の就寝・起床・YouTube の分値化。どちらも全日付を1回舐める。
  読み替えの要る日だけ複製し、**入力オブジェクトを書き換えない**(同じJSONを二度通すと二重に変換されるため)。
  v9 は変換の起点が旧キーの存在なので、新キーが再変換されることは構造的に起きない。
  旧キーと新キーが両方ある入力(手で繋いだJSON)は**新キーを優先**し、旧キーだけ落とす。
  自由入力の `weightVal` だけは例外で、`change` ハンドラで「数字.数字」に整形し、
  さらに `viewTrend()` の読み出し側で `isFinite()` を確認する。NaN を SVG の座標や目盛りに
  渡すと軸ラベルごとチャートが壊れるため、入口と出口の両方で止める。
- **`lastBackup` は「実際に持ち出せた」ときだけ更新する(v2.4、v2.8 で経路追加)。** JSONの
  クリップボードコピーが成功したとき、ファイル書き出し(`navigator.share`)が完了したとき、
  そしてPCへの同期が 200 を返したとき(受信側が書き終えた合図)の3つのみ。
  CSVは復元できないので数えず、共有シートを閉じた(AbortError)場合も数えない。
  過少申告は追加のバックアップを促すだけだが、過大申告は「守られている」という誤った安心を作る。
- **`syncToday()` は「直前の今日」を見ていた場合だけ日付を送る(v2.4)。** ホーム画面PWAは
  再読み込みされずに復帰しうるので、`sel` が前日のままだと翌朝の入力が前日の欄に入る。
  ただし遡及入力の途中で復帰した人まで今日へ引き戻すと入力先が黙って変わるため、
  `sel === lastToday` のときに限る。`lastToday` は `render()` が毎回更新する。
- **起床時刻(`wakeMin`)は計測のみ。目標・達成ライン・達成判定を持たせない。** 就寝と起床の両方を
  未達判定の対象にすると1日あたりの「失敗」の面積が二重になり、設計制約1・2(完璧主義への配慮)の趣旨と衝突する。
  記録の目的は睡眠時間の推定と起床時刻のばらつきの可視化であり、これは達成/未達の二値では表せない。
  よって `CORE` にも `targets` にも `th` にも入れず、`enabled` と `entries[].wakeMin` だけを持つ。
  評価色(達成の `--pine` / 超過の `--sienna`)も使わず、`--indigo` の濃淡だけで早い→遅いを示す。
  週報テキストには生の分布を渡し、解釈は Claude 側に置く。
  - **睡眠時間は記録タブに数値で出す(v4.18、2026-08-18)。`sleepMin(bed, wake)` が導く。**
    v3.1 までは「両端が選択肢の遅い側の端で実測ではないので、アプリが『◯時間』と断定すると
    推定値が実測に見える」を理由に数値を出していなかった。**この理由は v9(v4.0)で失効している**
    (就寝・起床が分の実値になった)。失効を確認したうえで出すことにした。
  - **v9 の移行値を含む日は「約」を付ける。判定は日付で行う(`isApproxSleep`)。**
    実データでは `25:12 → 8:12` が4日あり、**それがきっちり7時間になるのは変換の副産物**
    (両端に同じ丸めが入るため)。印を付けないと「7時間が並ぶ」見え方が実測だと誤読される。
    移行を通っていないと言い切れる日(2026-08-17 以降)は 5時間45分 / 6時間30分 で、どちらも7時間ではない。
    - **値だけでは移行値を見分けられない。** `BT_MIN9` / `WK_MIN9` の変換先は開区間の
      25:12 / 8:12 を除くと 23:00 / 23:30 / 24:00 / 24:30 / 6:00 / 6:30 / 7:00 / 7:30 で、
      **どれも select で選べる値**。「15分格子に乗らないこと自体が印」という既存の記述
      (v9 の項に今もある)は**チャートの見え方の話としては正しいが、日ごとの断定表示の
      根拠には使えない**。5バケット中4つを取りこぼす(2026-08-18、guardrail-reviewer の指摘で判明。
      最初の実装はこれを取り違えていた)。
    - よって `REAL_TIME_SINCE = "2026-08-17"` より前の日付キーは一律「約」にする
      (v4.0 のリリースが 2026-08-16 なので、その日までは移行を通りうる)。
      遡及入力で入れ直した日にも付くが、**誤る向きが安全側**(実測を概算と呼ぶだけで、
      変換値を実測として断定しない)。この日以降の記録には付かないので自然に消える。
    - 格子外の判定も OR で残す。取り込みJSON経由なら今後も格子外の値が入りうるため。
  - **行は起床時刻の直後、眠れた感の上に置く(オーナー判断 2026-08-18)。下へ戻さないこと**
    (テストで縛ってある)。**一度は下へ動かして、同じ日に戻した**経緯があるので、
    「主観の汚染を考えていなかった」と読まないこと。
    - 下げた理由: 数値を見た直後に主観を答える順になると、眠れた感が数値の追認になりうる。
      睡眠時間が直接導かれるのは「就寝×眠れた感」「起床×眠れた感」の2本だが、
      **眠れた感を含むペアは `defs` に10本ある**ので、汚染したときに効く範囲はそちら全部。
    - 戻した理由: **順序を下げても解消は部分的だった。** 起床時刻は眠れた感の直上にあり、
      起床を選んだ時点で再描画が走って睡眠時間の行が同じカード内に現れる。読む順が変わるだけで、
      同時に見えること自体は変わらない(guardrail-reviewer の指摘)。
      部分的な効果と引き換えに「入れた直後に見える」を落とすより、上へ戻すことをオーナーが選んだ。
    - **どちらの並びも guardrail-reviewer の審査を通っている**(2026-08-18、2回)。
      制約6は眠れた感を計測のみと定めるだけで、主観の独立性までは条文にないため、
      どちらも抵触ではないという判定。**並びだけを蒸し返さないこと。**
      汚染が実際に問題だと分かったら、順序ではなく「眠れた感が埋まるまで出さない」
      「別カードへ出す」など別の手段で解く(オーナーに相談してから)。
  - 注記は「移行前の記録が**含まれうる**ため概算」。アプリが知っているのは日付だけで、
    その日が実際に移行を通ったかは分からない(08-10 / 08-14 は両端とも格子上で区別できない)。
    **「含むため」と断定しないこと**(未確認を確定情報として書かない、の線)。
  - **保存しない。** 就寝と起床から常に導けるので、持つと就寝を直したときに古い長さが残る。
    台帳にも出ない(保存項目ではないため app-hub の許可リストは無関係)。
  - **達成ラインを持たせない(オーナー判断 2026-08-18)。** 就寝時刻に既に達成ラインがあり、
    睡眠時間にも置くと同じ夜の行動を1日に2回未達判定することになる(この項の冒頭と同じ理屈)。
  - **出す場所は記録タブだけ。** 週報には渡さない——週報は就寝・起床を昇順ソートして日付対応を
    落として渡すので、**あちらでは長さを計算できない**。混ぜると計算できたように見える。
    推移タブにも数値を並べない(28日ぶんの「◯時間」は読めない。帯で足りる)。
  - **成立しない値では行ごと描かない。** `migrate()` は entries を素通しするので手で編集したJSONから
    任意の値が来うる。上限は選択肢の両端から導く(数値を直書きすると範囲を広げたときここだけ古くなる)。
- **月間ビュー(`viewMonth`)は週タブのドット語彙を1ヶ月に伸ばしただけのもの。**
  出所は週タブと同じ `allItems` + `enabled` なので、達成判定を持たない項目(起床時刻・眠れた感・気分)は
  選択肢に出ない。**連続日数は出さない**(制約1)。**未入力は `miss` として未達と区別する**(制約2)。
  月ごとの目標は持たないので、達成数と記録数を事実として並べるだけにしてある。
  - **日ごとの入力量を濃淡で塗るヒートマップにはしない(検討して却下)。** 入力しなかった日が
    薄く塗られると、未入力ではなく「悪い日」に見える。制約2に正面から反し、
    見た目も貢献グラフに近く制約5に触れる。俯瞰は「1項目を選んで並べる」で足りる。
- **「今の気分」(`MOOD`)も同じ理由で計測のみ(schema 8)。** 主観状態なので目標・達成ラインを持たず、
  `CORE` にも `targets` にも `th` にも入れない。**週タブと目標にも出さない。**
  1日3回(`moodM` / `moodN` / `moodE` = 朝/昼/晩)その場でタップし、指す時点は `PERDAY` と同じく当日。
  - **`PERDAY` に入れないこと。** あちらは「記録した回がすべて達成なら達成」を畳む機構で、
    `CORE` に concat される。入れた瞬間に週目標と週タブの未達行が付いてきて制約6に反する。
  - **相関ヒントでは朝の値(`moodM`)だけを使う。** 前夜・当朝の要因と時間的に最も近く、
    昼・晩は日中の出来事が挟まって向きが読めなくなるため。昼・晩は週報に生の分布として渡す。
  - 気分のペアを足したぶん相関ヒントの行が増えた(本数の真実は `index.html` の `defs`)。
    行が増えるほど、どれか1本に偶然の相関が出る確率が上がる。**行あたりの `n` の表示と
    「両項目が揃った日が14日未満のペアは出さない」フィルタが、28日ロックに次ぐ防御。外さないこと。**
  - **並びは |φ| ではなく `strength()`(Fisher の z 変換による95%下側限界)の順。**
    |φ| そのままだと、項目を足すたびに記録し始めの行が上位を占め、たまたま大きく振れた値が
    最も目立つ場所に出続ける。表示する φ と n は実測のままで、**順番だけを確かさで決める**。
    同じ確かさなら記録日数の多い方を上に置く。`strength()` は `|φ|` が丸め誤差で1を超えた場合に
    NaN を返さないよう手前で止めてある(NaN は比較が常に false になり、並び順だけが黙って壊れる)。
  - **「朝の気分「高」× 晩の気分「高」」だけは分母が自己選択される。** 朝の項目は同じ入力で
    一緒に埋まるが、晩の枠は別の機会にアプリを開かないと埋まらず、開くかどうか自体が調子と相関しうる。
    データからは取り除けないので、条件をラベルに出して読む側に見えるようにしてある。ラベルを短くしないこと。
- **「外食」(`eatout`、schema 19、2026-08-17)も計測のみ。** 目標・達成ライン・達成判定を持たず、
  `CORE` にも `targets` にも `th` にも入れない。指す時点は前日。
  - **`meal`(食事の節制)とは独立。** `meal` の注記は「ジャンクフードと飲み会がなければ節制した」で
    外食が暗黙に混ざっているが、**外食先でも節制はできる**ので別の事実として持つ。
    片方から他方を導出しないこと。
  - **評価色を付けない**(`pairBtn` の `{neutral:true}`)。外食は良し悪しではないため、
    `companion` と同じ理由で全段を同色にする。**この中立化は opt-in で、既定の pine/gray は変えていない**
    (`sober` は従来どおり ✓ が達成側の色で出る)。
  - **ラベルから ✓ / ✗ も落とす**（「した / していない」）。このコードベースは ✓ を
    「達成側の印」として使っており、直上の `sober` は ✓ を良い側に保つためにラベルを
    反転させた項目。その真下に「外食 ✓ した」が並ぶと ✓ 側が目標に見える。
    **`pairBtn` を使う真偽項目のうち達成判定を持たないのは `eatout` だけ**なので、
    記号を外したことで「✓ ＝ 達成側の印」の規則は全項目で成立する側に揃った。
  - **週報テキストへ渡す**（記録日数と外食した日数）。渡さないとレビューを書く Claude 側は
    外食の存在を知り得ない。他の計測のみ項目と同じく、評価を禁じる注記を添える。
  - 目的は「外食した日の夜がどうだったか」を読むこと。献立アプリ(hatchmenu)の履歴は日付を持たず
    Aubade と結合できないため、この項目が唯一の突き合わせ経路になる。
    相関ヒントに「前日に外食した × 眠れた感「良」/ 朝の気分「高」」の2本を置いてある。
- **「誰と過ごしたか」(`companion`、schema 11)も計測のみ。** 目標・達成ライン・達成判定を持たず、
  `CORE` にも `targets` にも `th` にも入れない(起床時刻・気分と同じ扱い)。指す時点は前日。
  - **順序尺度ではない。** ひとり/家族/友人/仕事のどれが「良い」わけでもないので、
    評価色(`--pine` / `--sienna`)を使わず全段を同色にしてある。色を付けると
    「家族と過ごした日が達成」と読め、設計制約1・2の趣旨と衝突する。
  - **相手の名前も人数も持たない。** 契約上の制約ではなく(2026-08-16 の改定で身近な人のことは
    書けるようになった)、分類のままの方が気分との突き合わせに使えるため。名前は集計できない。
  - 目的は気分(`moodM` 等)との突き合わせ。気分は既に記録されているが「何がそれを作ったか」が
    無く、相手を足して初めて読める。週報テキストには分布と凡例を渡し、
    **「ひとりが多い週は良くない」のような価値づけを禁じる注記**を添えてある。
    順序尺度ではないので、これが無いと Claude 側が勝手に良し悪しを付ける。
- **水分(`waterMl`)は当日を指す計測のみ(schema 18、2026-08-17)。** 元は前日を指していたが、
  水は1日かけて飲むので他の項目のように翌朝まとめて思い出せない。前日のままだと今日ぶんを入れるのに
  **明日の画面へ移動する必要があり**、実際に翌日ぶんのエントリへ入力されていた(オーナーの申告)。
  記録タブでは `PERDAY` / `MOOD` と同じ当日カード群に「今日の水分」として置く。
  - **達成ラインを外した理由は「移した」ことの結果。** 当日を指す項目に閾値を置くと、
    まだ飲み終わっていない日中がずっと未達に見え、1日あたりの未達の面積が増える(設計制約1・2の趣旨)。
    `PERDAY` は「記録した回がすべて達成なら達成」なので閾値を持たず、`MOOD` は達成判定自体を持たない。
    よって `CORE` にも `targets` にも `th` にも入れず、`enabled` と `entries[].waterMl` だけを持つ。
    - **これは水分についての個別のオーナー判断(2026-08-17)で、一般原則ではない。**
      「当日を指す項目には閾値を持たせない」と読まないこと。同種の設計を選ぶ項目が出たら、
      そのつどオーナーに確認する(v4.14 の仕事の重さ「なし」と同じ書き分け)。
    週報テキストには**「1日2L」のような外部の基準を持ち込ませない注記**を添えてある
    (`companion` に価値づけを禁じる注記を付けたのと同じ理由)。
  - **`migrate()` の v18 で全日付を1回舐め、値を1日前のキーへ移す。** 移送で空になった日は
    キーごと消す(空のまま残すと「記録した日」に数えられる)。汚れた値と日付でないキーは動かさない。
  - **キー改名(v9 / v16 の手)を選ばなかった3例目。** 踏み切れたのは**台帳へ `waterMl` が
    1件も出ていないことを実測できた**ため(変換器は8日より古い日しか出さず、`waterMl` の新設は
    2026-08-16。`app-data/work/aubade-*-events.json` の全ファイルと台帳の写しを確認して0件)。
    v7 / v17 と同じ「窓」だが、あちらと違い**窓が開いていることをその場で数えられた**点が違う。
    出ていたら同一キーに2つの意味が混ざるので改名が要った。窓は 2026-08-24 に閉じる。
  - **`o.version` は `defaultData()` から取る**ようにした(v18 で `o.version = 17` の書き写しを
    上げ忘れ、移行が毎回走る形で静かに壊れかけたため)。ここに数値を書かないこと。
- **「ひとこと」(`memo`、schema 12)は朝の動線の外に置く。** 既定は畳んであり、
  開くまで textarea は DOM に存在しない。設計制約3は「**朝の入力**の摩擦を増やさない」話で、
  自由入力を全面禁止したものではない、という線で引いた(オーナー承認済み)。
  **既定で開いた状態にしないこと。** 開いた瞬間に朝の動線へタップ以外の作業が入り、制約3に触れる。
  - `enabled` にも `targets` にも入れない(`weightVal` と同じ、達成も計測もしない自由入力)。
    表示トグルも持たない。持たせると「書かなかった日」が未達に見え、設計制約2に反する。
  - **台帳(app-hub)へ出す**(オーナー判断、2026-08-16)。2026-08-16 の契約改定で
    他人の情報が許されたため。app-hub 側は唯一の文字列項目として個別に処理し、
    1000文字を超える日は落として警告に出す(取り込みJSONは手で編集できるので、そこが最後の関門。
    台帳は追記オンリーなので、入れた巨大な文字列は二度と取り除けない)。
    - **遡及入力の窓(7日)を過ぎると確定し、以後は書き直せない。** 記録タブの注記で
      利用者にも伝えている。文言を消さないこと(消すと「アプリで直したのに残っている」に
      後から気づくことになる)。
    - `entered`(その日どれだけ記録したか)には数えない。Aubade 側の「◯/◯ 入力」の分母にも
      入っていないので、数えると台帳と画面が食い違う。
  - **週報テキストには混ぜない。** 週報はチャットへ貼る動線なので、書いた散文がそのまま外部へ出る。
    台帳はこの端末とPCの中に留まるが、週報は違う。同じ「外に出す」でも線を分けてある。
    - **線は「自由入力かどうか」ではなく「散文か数値か」で引いている**（2026-08-17 に
      `weightVal` を週報へ出したときに明示化）。`memo` と `weightVal` はどちらも自由入力・
      達成判定なしだが、散文は書いた本人の文脈ごと外へ出るのに対し、数値は分布としてしか出ない。
      **次に自由入力を足すときは、散文なら週報に混ぜない。**
      なお週報に出した数値は Claude.ai の履歴に残り、後から取り消せない。
  - 持ち出しはバックアップJSONが担う。PCへ同期していれば受信先の `app-data` に残る。
  - `maxlength="300"` は同期の payload を無制限に膨らませないため。保存のたびに全履歴を送る。
- **勉強は分の実値 `studyMin`(schema 14)。** 真偽値 `study`(15分以上したか)から移した。
  達成ラインは `th.studyMin`(既定15分)で、**この値以上で達成**。指す時点は前日。
  - **3例目のキー改名**(v7 の歩数、v9 の就寝・起床に続く)。改名により台帳の上で
    旧キー(真偽)と新キー(分)が別フィールドになり、意味が混ざらない。
  - **`migrate()` で変換しない。** ここが v9 と違う。`study: true` が意味するのは
    「15分以上」という**下限**であって実測値ではない。`studyMin: 15` を書き込むと
    15分格子の上に乗り、実測と見分けが付かなくなる。v9 の就寝時刻は変換値が格子から
    外れること自体が印になったが、勉強ではその印を作れない。だから entries は触らず、
    **読み出し側の `studyDone(data, entry)` が両方を見る**。推移と週報の分布には
    `studyMin` の実測だけが入り、旧データが偽の点として混ざらない。
  - **達成判定の出所は `studyDone` だけ。** `achieved()` も相関ヒントもここを通す。
    相関ヒントが `e.study` を直接見ていた箇所を移行時に直した(見落とすと相関ヒントだけ静かに欠ける)。
  - 両方あるエントリは**新キーを優先**(手で繋いだJSON対策。v9 の旧キー/新キーと同じ向き)。
  - CSV は旧列「勉強(旧・真偽)」を残したまま「勉強(分)」を足してある。
    旧列を消すと v4.7 より前の日の記録が書き出せなくなる。
- **瞑想は分の実値 `meditationMin`(schema 20、2026-08-18)。** 達成ラインは `th.meditationMin`
  (既定10分)で、**この値以上で達成**。指す時点は前日。`CORE` / `targets` / `th` のすべてに乗る
  (勉強・ジム・サウナと同じ扱い)。
  - **出所は app-hub の momentum。** あちらでは習慣の1つとして持っていたが、台帳へ出るのが
    `habit_checked {habit_id:"<UUID>"}` だけで、**名前は端末の localStorage にしかなかった**。
    習慣を消すと過去のイベントは永久に解読不能になる(台帳は追記オンリー)。
    実測(2026-08-18)では momentum の習慣5件のうち4件が Aubade / Tonnage と二重管理で、
    前後と独立した「本物の1タップ」は瞑想の1件だけだった。**移したのはその1件。**
  - **`migrate()` に変換ブロックは要らない。** 勉強(`study`→`studyMin`)と違い旧キーが無いので、
    `Object.assign` の既定値埋めだけで足りる。読み出し側に `studyDone` 相当の関数も要らない。
  - **選択肢は5分刻み(`MEDITATION_MIN`)。** 勉強の15分刻みでは粗すぎるため
    (瞑想は5〜30分が実勢)。**細分化してよいのは物理量だけ**という既存の線の内側にある。
    ただし**達成ラインの選択肢は5/10/15/20/30/45/60**で、入力より粗く保つ
    (5分単位で締め上げられる状態を作らないため。就寝・YouTube と同じ理屈)。
  - 相関ヒントは眠れた感と朝の気分の2本。瞑想を足した目的が主観状態との突き合わせなので、
    対もそこに置いてある。**`defs` は手書きで、足し忘れても何も落ちない**ため
    `pwa.test.js` に文言のガードを置いた。
- **昼寝は分の実値 `napMin`(schema 21、2026-08-18)。目標・達成ライン・達成判定を持たない計測のみ。**
  `CORE` / `targets` / `th` の**どれにも乗せない**。持つのは `enabled` と `entries[].napMin` だけで、
  起床時刻(`wakeMin`)・外食(`eatout`)と同じ枠。
  - **指す時点は当日(schema 22、2026-08-18)。** 新設時は前日を指していたが、同じ日のうちに移した。
    昼寝は日中に起きるのに、勉強・瞑想と違って**タイマーを持てない**(アプリは通知を持たないので、
    鳴らないタイマーでは昼寝から起きられない)。前日のままだとその場で入れる経路が無く、
    今日ぶんを入れるのに明日の画面へ動く必要があった——**水分(`waterMl`、schema 18)で実際に
    翌日ぶんのエントリへ入力されていたのと同じ動線**。記録タブでは水分の直後、当日カード群に
    「今日の昼寝」として置く。
    - **`migrate()` の v22 で全日付を1回舐め、値を1日前のキーへ移す**(v18 と同じ手をなぞってある)。
      移送で空になった日はキーごと消す。汚れた値と日付でないキーは動かさない。
      達成ラインは元から持たないので、水分と違い `th` / `targets` からの削除は要らない。
    - **キー改名(v9 / v16 の手)を選ばなかった4例目。** 踏み切れたのは**台帳へ `napMin` が
      1件も出ていないことを実測できた**ため(2026-08-18。`app-data/work/aubade-*-events.json` の
      全ファイルと台帳の写し全48イベントを確認して0件。新設が同日で、変換器は8日より古い日しか出さない)。
      出ていたら同一キーに2つの意味が混ざるので改名が要った。`waterMl` と同じ「窓」で、
      **窓が開いていることをその場で数えられた**のも同じ。窓は 2026-08-26 ごろ閉じる。
    - 移行対象の実データは 2026-08-18 の1件(`napMin: 10` → 2026-08-17 へ)。移行後に実バックアップを
      通して確認済み。
    - **これは昼寝についての個別のオーナー判断(2026-08-18)。** 同じ問いは勉強・瞑想にもあるが、
      あちらは**達成ラインを持つ**ので当日へ移すと「まだ終わっていない日中がずっと未達に見える」
      (水分で閾値を外した理由と同じ)。移していないのはそのため。**3項目まとめて動かさないこと。**
    - **入力するのは「昼寝から起きた直後」(オーナー確認 2026-08-18)。** 当日を指す設計はこれを
      前提にしている。翌朝まとめて入れる運用だと、この項目だけ日付移動の2タップが要るうえ、
      `syncToday()` は `sel === lastToday` のときしか今日へ引き戻さないので戻し忘れが起きる
      (guardrail-reviewer がグレーとして挙げた点。前日を指していたときの「明日の画面へ動く」と鏡像)。
      **入力のタイミングが変わったら、指す時点も再検討する。**
    - **昼寝しなかった日は空欄のまま(オーナー確認 2026-08-18)。** `NAP_MIN` は0分を選べるが、
      「していない」を毎日入れる運用にはしない。未入力は失敗ではない(制約2)の線で、
      当日カードに空欄の枠が残ることを許容する。**「今日はまだ入力していません」のような
      催促を足さないこと**(制約5)。相関ヒントの分母には入らない。
  - **瞑想(`meditationMin`)に混ぜてはいけない。** 3つの理由があり、どれか1つでも十分:
    (a) 達成ラインの向きが逆 — 瞑想は「10分以上で達成」だが、昼寝は長いほど良いわけではない。
    混ぜると40分寝落ちした日が「瞑想40分＝達成」として数えられ、達成率が意味を失う。
    (b) 相関ヒントが相殺する — 瞑想は夜の睡眠に中立〜正、昼寝は睡眠圧を下げるぶん負に効きうる。
    同じフィールドだと φ が 0 に寄り「効果なし」と読める。瞑想を足した目的そのものが壊れる。
    (c) 台帳は追記オンリーで、混ぜた日は後から分離できない(momentum から移した理由と同じ轍)。
  - **目標を持たせない理由。** 昼寝は前夜の睡眠次第で最適量が変わり、達成/未達の二値で表せない。
    目標にすると疲れていない日にも昼寝する動機が生まれ、夜の睡眠を削る(計測装置が行動を歪める)。
    `wakeMin` の条文と同じ趣旨で、未達の面積を増やさない(設計制約1・2)。
  - **相関ヒントの二値化は「した/していない」(`napMin>0`)。** 達成ラインが無いので `th` は見ない。
    対は3本。眠れた感・朝の気分がコスト側(夜の睡眠への影響)で、**期待する向きは瞑想と逆でよい**。
    - **効果側の対は「その日の晩の気分」1本だけ(v4.21)。** 日中の集中力・脳疲労を測る項目は持たないので、
      効果側に届くのはこれが唯一。**新しい主観尺度は足さない** — 昼・晩の気分が既にあり、
      同じことを別の枠で聞き直すことになる(実測 2026-08-18: 気分の運用が始まった 8/15 以降は
      昼 4/4日・晩 3/4日 埋まっている。埋まらないのが問題なのではなく、時点がずれていたのが問題だった)。
    - **昼ではなく晩を使う。** 昼寝は当日の昼〜夕に起きるので、昼の気分では昼寝より前かもしれない。
      晩なら確実に後になる。晩の枠の自己選択(別の機会に開かないと埋まらない)は
      朝×晩の対と同じくラベルに出す。
    - **ラベルは時点の関係を出す**(「昼寝した × その夜の眠れた感」「× 翌朝の気分」「× その日の晩の気分」)。
      当日を指すようになったので「前日に昼寝した」は誤りになった。
    - **効果側の1本だけが `mk` で作れる。** `napMin` と `moodE` はどちらも当日を指すので
      同じ日付キーに入る(schema 22 以降)。**コスト側の2本は逆に `mkNext` が要る** —
      `napMin` は d の昼寝で、その夜の眠れた感と翌朝の気分は `entries[d+1]` にあるため。
      間違えても n=0 になるのではなく**1日ずれた対が黙って作られる**ので、`pwa.test.js` にガードがある。
      **schema 22 で向きが逆転した**(それ以前は効果側が `mkPrev`、コスト側が `mk` だった)。
    - **交絡は解けない。疲れた日ほど昼寝するので、効果が実在しても φ は 0 か負に寄る。**
      これは項目をいくつ足しても解けない。潰すには昼寝する日を疲労度ではなく曜日で決める
      (N-of-1。アプリの外の運用で、`BACKLOG.md` の運用の決定にある)。
    - **`foot` は2文に分ける。** 「日中の集中力・脳疲労は測っていません。疲れた日ほど昼寝しているなら、
      関係が出なくても、効果が無いという意味ではありません。」**前半はアプリの構造上の事実**
      (晩の気分は主観の総合であって日中の集中力ではない)、**後半はオーナーの行動についての推測**で、
      確かさが違う。推測を断定形で書かない(睡眠時間の「約」と同じ線)。
      どちらか片方を落とすと、効果があるかもしれない側だけを弁護する一文になる
      (guardrail-reviewer の指摘 → オーナー確認済み、2026-08-18)。
    - **`mkNext` を使う対は2本だけに固定してある**(`pwa.test.js` が出現回数を縛る)。
      `fa`=当日を指す項目 / `fb`=翌日のエントリから取る項目 という前提はシグネチャから読めず
      コメントにしかないので、3本目を足したときに必ずテストを落として時点の前提を読み直させる。
      **ずれても行は消えず、1日ずれた対が黙って作られる**ので、気づく経路がこれしかない。
    - **出し分けの条件は `enabled` ではなく「昼寝の行が実際に出たか」(`res` を見る)。**
      相関ヒントの `defs` は `enabled` を一切見ないので、`S.enabled.napMin` で出し分けると
      表示トグルを off にしたとき**行だけ残って注記が消える**(守る対象が画面に残ったまま守りが外れる)。
    - **`foot` の中立性は先頭の「相関であって因果ではありません」に依存する。**
      段落を縮めて昼寝の1文だけを残すと、効果があるかもしれない側だけを弁護する文になる。
  - **記録タブでは水分と同型のカードにする**(カード見出し `dayCard("昼寝")` / 行は「合計」/
    `foot` で「達成/未達はありません」と閾値を置けない理由)。**当日カード群には閾値を持つ行が無い**ので、
    前日カードにいたとき(真上が「10分以上で達成」の瞑想)に要った行内の「(計測のみ)」は
    `foot` の1文へ移してある。行のラベルから「計測のみ」を消すときは、`foot` が残っているか見ること。
  - **週報テキストに「夜の睡眠に逆向きに効きうる」を渡さない。** フィールドを分けた理由は
    実装側の判断で、レビューを書く Claude に渡すと直前の「評価しないで」と逆向きのプライミングになる。
    週報は【来週の一点】を本人に返す動線なので、「昼寝を短くする」が一点として出る経路が開く。
    渡すのは「瞑想とは別の項目なので合算しないでください」まで。
  - **昼寝を対象にした【来週の一点】は書かない**(オーナー確認済み)。業務時間・仕事の重さと同じ
    「他項目の説明変数」の位置づけ。28日棚卸しで「一点を書けないから外す」と判断しないこと
    (`BACKLOG.md` の運用の決定に1行残してある)。
  - **タイマー種別は持たせない。** `TIMER_KINDS` は足せる作りだが、アプリは通知を持たない
    (設計制約5)ので鳴らないタイマーでは昼寝から起きられない。用途を満たさない機能は足さない。
  - 週タブのドット行も持たない(`eatout` に倣う)。月間ビューは `allItems` から導くので自動的に出ない。
  - 選択肢は5分刻みで上限2h(`NAP_MIN`)。実勢は5〜20分だが、寝落ちが天井に張り付くと
    実際の長さが記録から消えるため上を広く取ってある(`WATER_ML` と同じ理屈)。
- **タイマーは種別を持つ(schema 20 と同時、v4.17)。** `TIMER_KINDS` = 勉強(`studyMin`) /
  瞑想(`meditationMin`)。**記録先フィールドとラベルの出所は `TIMER_KINDS` だけ**にしてあり、
  タイマータブの累計表示もここから導出する(分岐に書き写すと種別を足したとき片方だけ古いまま残る)。
  - **同時走行は許さない(排他)。** 状態は単一オブジェクトなので、2本目を許すと
    どちらの経過なのか状態から復元できない。描画は走行中に開始ボタンを出さないが、
    **ハンドラ側にも防壁を置いてある**(描画だけを直しても、取り残されたボタンや二度押しで通るため)。
    弾いたときは flash で理由を出す(押したのに何も起きないと、壊れたのか仕様なのか分からない)。
  - **`kind` を持たない走行状態は勉強として読む。** v4.16 以前は勉強専用だったので、
    デプロイを跨いで走っていたタイマーには `kind` が無い。`loadTimer()` はホワイトリスト方式なので、
    ここで補わないと記録先が黙って消える。
  - `timerTarget()` は種別で分岐しない。**どちらも前日を指すため。**
    当日を指す項目にタイマーを付けるなら、ここが分岐点になる。
- **勉強タイマー(v4.8)は「経過を数えない」。** 開始時刻だけを持ち、止めるときに
  `Date.now() - startedAt` で出す。**`setInterval` で数えてはいけない。**
  iOS のホーム画面PWAはバックグラウンドで動けないので、画面を消した瞬間に計数が止まり、
  実際より短い時間が記録される(勉強中に画面を消すのは普通のこと)。
  - **状態は本体データと別キー `flourish-log-v2-timer`。** 走行中の状態は記録ではないので、
    バックアップJSON・週報・台帳のどれにも混ぜない。`migrate()` の対象外。
    壊れていたら握りつぶさずログを出して null に倒す(タイマーが復元できないだけで起動は妨げない)。
  - **記録先は「開始した日の翌日」ぶんのエントリ。** `studyMin` は前日を指すため。
    日付は**開始時点で決める**ので、日をまたいで止めても始めた日の勉強として数える。
  - **停止の押し忘れに上限(`TIMER_MAX_MIN` = 6時間)を置く。** 超えた経過は記録せず理由を出す。
    止め忘れは「実際より長い」方向にしか壊れず、黙って足すと週の合計が一度で狂ううえ、
    後から見て異常だと気づけない。開始の押し忘れは手動入力(select)で補えるので上限は要らない。
  - **独立したタブに置く(v4.9)。記録タブには一切出さない。**
    朝の入力と勉強中の計測は使う時間帯も動線も違い、同じ画面に混ぜると
    朝の一覧にタイマーが並ぶ(設計制約3)。v4.8 では記録タブの上下に置いていたが、
    タブを分けて完全に外した。**記録タブへ戻さないこと**(テストで縛ってある)。
    タイマータブには記録先の日付と今日ぶんの累計も出す(どこに入るか分からなくなるため)。
  - **経過表示は毎秒更新する(v4.10)。ただし `setInterval` は「表示を引き直す」ためだけに使い、
    経過を数えさせない。** `timerPaint()` は毎回 `startedAt` から計算し直すので、
    バックグラウンドで tick が止まっても復帰後の最初の1回で正しい値へ戻る。
    数える実装にすると、止まっていた時間がそのまま欠測になる(この違いをテストで縛ってある)。
    - 更新は経過の桁(`#timerElapsed`)だけを書き換える。`render()` を毎秒呼ぶと innerHTML を
      丸ごと差し替えることになり、select を開いている最中に閉じるなどの実害が出る。
    - タイマータブ以外に移ったら止める。復帰時(`visibilitychange`)は即座に引き直す
      (interval はバックグラウンドで間引かれるため、これが無いと古い数字が残って見える)。
    - 走行中はストップウォッチ表記(`fmtClock`、分:秒 / 時:分:秒)。記録値の `fmtDur` とは別。
    - **毎秒更新は記録の精度に寄与していない**(オーナー判断で残している、2026-08-16)。
      記録は分単位、達成ラインは15分刻み、しかも `visibilitychange` で復帰時に引き直すので、
      毎秒更新が無くても「開いた瞬間の値」は正しい。得ているのは「動いているのが見える」体感だけで、
      guardrail-reviewer は制約5(演出)のグレーとして挙げた。オーナーが明示的に要望した機能なので残すが、
      **これは機能ではなく体感のための表示だと分かったうえで置いている**。
      画面内で最大のフォント(34px)かつ唯一動く要素であり、「静かで淡々とした計測器」(§4)とは摩擦がある。
      **外すなら `timerTickStart` / `timerTickStop` の呼び出しを消すだけでよい**
      (描画時と `visibilitychange` 復帰時の `timerPaint()` は残るので、秒表示という事実提示は失われない)。
      判断の分かれ目は運用で、勉強中にこのタブを開いたままにするなら実利があり、
      開始してすぐ閉じるなら実利はゼロ。
    - **瞑想(v4.17)でも種別で分けず、そのまま出す(オーナー判断、2026-08-18)。**
      guardrail-reviewer は「瞑想は目を閉じて端末を伏せる運用なので、上の基準に照らすと
      実利ゼロ側にほぼ確実に落ちる」とグレーで挙げたが、オーナーは両方そのままを選んだ。
      **再検討するときはこの判断を蒸し返す前に、前提が変わったかだけ確認すること。**
  - タイマーと手動入力は同じフィールドを指す。タイマーは加算、select は上書き。
    47分のような格子外の値は `minSelect()` が option を足して選択状態にする。
- **仕事の重さ(`workLoad`)は「なし」+ 3段階(schema 17)。** `LOAD_OPTS` = なし/軽い/普通/重い。
  「なし」は**尺度の点ではなく「その日は仕事をしていない」**で、良し悪しの外にあるため評価色を付けない
  (軽い=`--pine` / 重い=`--sienna` はそのまま)。
  - **業務時間(`workMin`)を 0 にすると自動で「なし」が入る。** 0 は「仕事をしていない」で確定するので
    もう1タップ求めない。**ただし空欄のときだけ埋める。** 無条件に入れると、本人が「重い」を
    選んだあとに業務時間を 0 へ訂正しただけで、入れた値が黙って置き換わる。保存値からは
    本人の入力かアプリが入れたかを区別できず、台帳にもそのまま出る
    (`study: true` を `studyMin: 15` へ変換するのを拒否したのと同じ理由)。
  - **「軽い/普通/重い」の3段はこれ以上増やさない。** 主観尺度の細分化は疑似精度と自己評価圧を
    増やすだけ(下の項)。テストで縛ってある。
  - **「なし」の追加は個別のオーナー判断(2026-08-17)で、一般原則ではない。**
    「尺度の外の段なら足してよい」と読まないこと。同種の要望(眠れた感に「寝ていない」、
    気分に「該当なし」など)が出たら、そのつどオーナーに確認する。
  - 先頭に足したので既存の 軽い0/普通1/重い2 は 1/2/3 へずれる。`migrate()` の `from < 17` で読み替え。
    **踏み切れたのは台帳が `workLoad` を1件も持っていない窓だったため**
    (2026-08-17 に実測: 台帳の Aubade イベントは 2026-08-08 / 08-09 の2件のみで `workLoad` は0件、
    端末スナップショットにも旧スケール値なし)。**v9 のキー改名と違い、これは構造的な安全ではない。**
    同一キーのインデックス再割当てなので、安全を担保しているのは実測した時点の事実だけ。
    v7 の歩数と同じ「賭け」で、次に同じことをするなら窓はもう無い。
    - **端末を v4.14 へ更新するまで仕事の重さを入力しないこと。** v4.13 のまま入れた値が
      8日後に cutoff を越えると、旧スケールのまま台帳へ入って取り返しがつかない。
  - 相関ヒントは「重い」を**インデックス直値**で見ている(`workLoad===3`)。段を動かすとここが黙ってずれる。
- **インデックスで保存する項目の刻みを後から変えない。** 対象は `companion`(誰と)と
  主観尺度の `SLEEP_OPTS`(眠れた感)/ `MOOD_OPTS`(気分)/ `LOAD_OPTS`(仕事の重さ)。保存されるのはインデックスなので、
  境界を動かすと過去の記録の意味が黙って変わる。変える場合は `migrate()` での読み替えとセットで設計すること。
  - **実値で保存する `bedtimeMin` / `wakeMin` / `youtubeMin`(v9)、`waterMl` / `workMin` / `stepsCount`(v16)は
    この制約の外にある。** 対応する `BEDTIME_MIN` / `WAKE_MIN` / `YT_MIN` / `WATER_ML` / `WORK_MIN` / `STEPS_N` は
    画面の都合にすぎず、刻みを増減しても過去の記録の意味は動かない。
    migrate も要らない。ただし**細分化してよいのは物理量だけ**で、主観尺度(眠れた感・気分)の段数は変えない。
    主観に外部の実値は存在せず、細かくしても精度は上がらず疑似精度と自己評価圧だけが増えるため。
  - **推移タブのY軸ラベルは左余白(44px)の内側に収める。** はみ出しても SVG は黙って左端で切るだけなので、
    「20000歩」の先頭が消えても数字に見えてしまう。JSDOM は文字幅を持たないので
    `npm run smoke` の「Y軸ラベルが左余白に収まる」が実測で見張っている。
  - **`fmtSteps` は歩数をそのまま出す(v4.12)。** 「2千」「1万」の丸め表記は、
    選択リストが1000歩刻みになった時点で目盛りと選択肢の粒度が食い違う
    (1000〜9000 が「1千」〜「9千」と並ぶ)。選択リストの最上段だけは上限ではなく
    「これ以上」なので `fmtStepsOpt` が「20000歩以上」に差し替える。軸と達成ラインには付けない
    (軸の目盛りは上限ではなく、達成ラインは「から達成」が既に下限を表しているため)。
  - **一度だけ破った記録(schema v7、2026-08-14)。** `STEP_OPTS` を3段階から4段階
    (`3千以下 / 5千 / 8千 / 1万以上`)に刻み直した。読み替えは `entries[].steps` と `th.steps` の一律 +1。
    旧0/1/2 は各段の**上限**が新1/2/3 と一致するので実際の歩数帯は動かず、過去の達成/未達も1つも変わらない
    (`defaultData` の `th.steps` も 1→2。意味は「8千から達成」のまま)。新設の「3千以下」は今後の入力にだけ現れる。
    このとき踏み切れたのは、app-hub の台帳にまだ Aubade のイベントが入っていない窓だと判断したため。
    **2026-08-16 に賭けの成立を確認した。** 台帳へ出た最初のイベント(2026-08-08 ぶん)の `steps` は
    新スケールの値で、旧スケールは1件も入っていない。遡及入力の窓(8日)を最初の記録が越えて
    確定扱いになった日が、v3.0 のリリース後だったため。**次に同じことをするなら窓はもう無い。**
    一般に安全になったわけではない。次に刻みを変えるときは (a)`migrate()` の読み替え
    (b)`version` の引き上げ (c)app-hub 台帳との整合 の3点セットで設計すること。
    台帳は追記オンリーで、入ってしまった旧スケールの値は後から直せない。
  - **2例目(schema v9、2026-08-16): 就寝・起床・YouTube をインデックスから分の実値へ移した。**
    キー名も `bedtimeMin` / `wakeMin` / `youtubeMin` へ変えてある。v7 のような「窓」は無いのに
    踏み切れたのは、**改名により台帳の上で旧キー(インデックス)と新キー(分)が別フィールドになり、
    同一キーに2つのスケールが混ざることが構造的に起きない**ため。これがインデックスの再割当てではなく
    キー改名を選んだ実利で、次に同種の変更をするときも改名を選ぶこと。
    移行値は各バケットの上端で、開区間(「以降」「2h超」)だけは旧チャートの慣行値(25:12 / 8:12 / 150分)。
    **これは実測ではなく変換値**で、15分格子に乗らないこと自体が印になっている。
    格子外の保存値には `minSelect()` が option を動的に足す。足さないと値があるのに無選択で描かれ、
    別の値を選んだ瞬間に黙って消える。
    3点セット (a)`migrate()` の読み替え (b)`version` の引き上げ (c)app-hub の許可リスト先行反映、は v7 と同じ。
- **Service Worker はネットワーク優先を崩さない。** キャッシュ優先にすると「古いHTMLを掴み続けて
  版が上がらない」事故が起きる。Service Worker を入れずにきた理由がそれなので、速度ではなく
  更新の確実さを取る。得られるのはオフライン起動だけでよい。
  キャッシュ名 `aubade-vX.Y` にはバージョンを含め、リリース時に上げる(`activate` の掃除がこれで効く)。
  `sw.js` は**ページのCSPの外側**で動くため、他オリジンへは自分から触らない(`self.location.origin` で弾く)。
- **通信は「オーナー自身のPCへ送る1本」だけ(v2.8)。** v2.7 までは `connect-src 'none'` で
  外部通信ゼロだった。PCへの同期を入れるにあたり `connect-src https://*.ts.net` まで緩めてある。
  緩和先が Tailscale のドメインなのは、**tailnet に参加している端末からしか名前を引けず、
  インターネットからは到達できない**ため。ホスト名を直接書かないのはこのリポジトリが Public だから。
  - `fetch` は `syncPush()` の1箇所だけ。テスト「index.html の通信は同期の1本だけで、
    送信先はハードコードされていない」が呼び出し回数と宛先(`fetch(syncCfg.url,`)を数えているので、
    2本目を足すと落ちる。**通信を増やすときは、なぜ増やすかをここに書いてからにすること。**
  - `XMLHttpRequest` / `WebSocket` / `sendBeacon` は引き続き全面禁止。
- **同期の設定は本体データと別キー `flourish-log-v2-sync` に置く(v2.8)。** 同じ blob に入れると
  トークンがバックアップJSONに混ざり、クリップボードコピー・ファイル共有・週報のたびに一緒に出ていく。
  別キーなので `migrate()` の対象外でもあり、**他人のJSONを取り込んでも送信先は書き換わらない**。
- **同期の成功では `save()` を呼ばない(v2.8)。** 成功時に `lastBackup` を更新するため、
  `save()` を使うと 保存 → 同期 → 保存 … と再帰する。書き込みだけを行う `persistQuiet()` を通すこと。
- **`fetch(...).then(ok, err)` の `ok` 内で throw しない(v2.8)。** `err` は元の Promise の
  拒否しか受けないので、成功側で投げると未処理の rejection になり、失敗表示に到達しないまま消える。
  失敗の扱いは `syncFailed()` に集約してある。

---

## 4. デザインシステム(「朝靄の計器盤」)

静かで淡々とした計測器。装飾より可読性。ダークモード非対応(朝専用アプリのため意図的)。

色・タイポ・コンポーネントの実値は `index.html` の `:root` と `<style>` が唯一の出所。
そこから読み取れない約束だけを書く。

- **新しい色を増やさない。** 既存トークンで表せないなら、まず表現の方を見直す。
- `--sienna` は「超過・注意」。**赤い警告色は使わない。**
- `--dim` は非活性と未達。`--pine` は達成と主ボタン。`--indigo` は夜・睡眠系。
- **外部フォントを読み込まない**(オフライン性と CSP のため)。数値は `.mono` + `tabular-nums`。
- 選択状態には `aria-pressed` を必ず付与する(テストとアクセシビリティの両方が依存)。
- **記録タブの記入欄と select は font-size 16px 以上にする(v4.23)。** iOS は 16px 未満の
  入力欄にフォーカスすると画面を拡大し、**focus を外しても戻らない**ので、以降その画面が
  拡大したままになる。**範囲は「朝の動線に出るもの」**。記録タブ以外(設定タブの同期URL・
  トークン・取り込みJSON・達成ラインの select・カスタム追加欄、推移タブの月間ビューの select)は
  据え置き(オーナー判断 2026-08-20)。**該当する class を数え上げないこと** —
  記入欄を足したときに、この条文だけが古くなる。
  - **`.memo` は汎用の `textarea` 規則より後ろに置くこと。** 実ブラウザは詳細度で解くので
    順序に関係なく勝つが、**JSDOM は詳細度を見ず後勝ちで解く**。前に置くとテストだけが赤くなる。
    **一致するのは font-size だけで、「JSDOM の値＝実機の値」ではない。**
    `padding` は今も両者でずれている(実機は `.wv` の `8px 12px`、JSDOM は `textarea` の `8px`)。
    JSDOM の computed style を実機の値の根拠にしてよいのは、勝つ規則が
    「最も詳細度が高く、かつ最後にある」ときだけ。
  - 実機での実効値は `npm run smoke` が computed style で見る。**この検査は CSP の一連の
    検査より後ろに置くこと** — 前に置くと `*.ts.net` の2件が時々落ちる(11回中2回)。
    Service Worker がページを claim した後の POST は `page.route` を通らず実DNSへ出るためで、
    CSP の検査までの間に操作を挟むほど claim が間に合ってしまう。

---

## 5. テスト(開発の契約)

```bash
npm install
npm test        # vitest。ロジックとDOM操作
npm run smoke   # 実ブラウザ(Chromium)。描画・保存・CSP・Service Workerの実効性
```

- 方式: 出荷物である `index.html` そのものを `JSDOM(html, {runScripts:"dangerously"})` で起動し、
  実DOMのボタンを `.click()` して `localStorage` の中身と再描画結果を検証する。
  復元テストは `beforeParse(w)` で localStorage をシードして「再起動」を再現。
- 範囲は `pwa.test.js` の `describe` 名が一覧になっている。読めば分かるのでここには列挙しない。
- 非同期を検証するテストは `boot(seed, prepare)` の `prepare(w)` で起動前の環境
  (壊れた保存データ、`Storage.prototype.setItem` の失敗)を作り、`stubClipboard` + `await tick()` で待つ。
- **CSPの実挙動は JSDOM では検証できない。** JSDOM は CSP を解釈しないため、vitest 側は
  meta タグの内容一致までしか見ていない。実効性は `npm run smoke` が担当し、
  記録を載せた fetch/XHR/beacon/画像/フォームを実際に投げて受信側に0件であることを確認する。
  **CSPを触ったら `npm run smoke` を回すこと。**
- JSDOM の癖: `document.visibilityState` の既定は `"visible"` ではなく `"prerender"`。
  復帰の再現には `Object.defineProperty` で差し替える。時刻は `withClock()` で `window.Date` を差し替える。
- **ルール: 全ての変更はテスト追加・更新とセット。デプロイ前に all green。**
  UIラベルはテストが `textContent` 一致で参照している(例: 「✓ した」「取り込む」「CSVをコピー」)。
  文言変更時はテストも更新すること。

---

## 6. デプロイと運用

- GitHub Pages。`main` の `/`(ルート)を配信元にしてあるので、`index.html` を push → 数分で自動反映。
- **リリース手順は `/release` スキル**(`.claude/skills/release/SKILL.md`)にある。手順なので CLAUDE.md には置かない。
- **iOSの容器に注意**: ホーム画面追加したPWAとSafariタブは localStorage の容器が別。
  オーナーの運用は「ホームのアイコンから開く」に固定されている。デバッグで挙動差を見たらまずこれを疑う。
- 古いHTMLをホーム画面PWAが掴み続けることがある(キャッシュ)。Service Worker はネットワーク優先なので
  通信できていれば必ず新しい方が出る。バージョン表記が上がらない場合は一度Safari側で開いて更新する。
- **オフラインでも起動できる(Service Worker 導入後)。** Chromium で確認済み
  (`npm run smoke` の「オフラインでも起動する」)。**iOS Safari とホーム画面PWAでの挙動は未確認**で、
  実機で機内モードにして開けるかはオーナーに見てもらう必要がある。
- バックアップ: 設定タブ「JSONをコピー」(オーナーへは月1回程度を推奨済み)。復元は「取り込む」。
- **このリポジトリは Public。** Pages を無料プランで公開するための選択。
  **リポジトリおよび本ドキュメントに個人情報を追記しないこと**(現状の内容はコードと設計判断のみ)。
  記録された実データは端末の localStorage にのみ存在し、リポジトリには一切入らない。

### 脅威モデル(何を守り、何を守らないか)

サーバーも認証もアカウントも持たない。守る対象は「記録が**オーナーの管理外へ**出ないこと」。
v2.7 までは「外へ出ないこと」だったが、v2.8 でオーナー自身のPCへ送る経路を1本だけ開けたため、
線の引き方がここで変わっている。

**守っていること**
- 記録の外部送信。CSP `connect-src https://*.ts.net` により、**許可した1ドメイン以外**への
  fetch/XHR/WebSocket を、`img-src data:` により画像ビーコンを、`form-action 'none'` により
  フォーム送信を遮断。実ブラウザで「攻撃者サーバーへの到達 0件」を確認済み(v2.2、v2.8で再確認)。
- 緩和先の到達範囲。`*.ts.net` は Tailscale のドメインで、通常は tailnet に参加している端末
  からしか到達できない。ただし **Funnel を有効にしたホストはインターネットからも到達できる**ので、
  「絶対に外へ出せない」とまでは言えない。ここが守っているのは「宛先が Tailscale のホストに限られる」
  ところまでで、`index.html` を書き換えられる攻撃者は CSP 行ごと書き換えられるため対象外
  (`sw.js` と同じ信頼レベルの話に帰着する)。
  `npm run smoke` が「*.ts.net には通る / それ以外は遮断される」の両方を実ブラウザで確認する。
- 送信先とトークンの秘匿。コードには書かず(このリポジトリは Public)、設定タブから入力して
  別キーに置く。バックアップJSON・CSV・週報テキストのどれにも混ざらないことをテストで縛っている。
- Service Worker 経由の迂回。`worker-src 'self'` は同一オリジンのスクリプトしか許さず、
  攻撃者がリポジトリに書き込めない限り新しい Service Worker を登録させる経路はない。
- 取り込みJSON由来のHTML属性注入。`esc()` + `migrate()` の型正規化で塞いでいる。

**守っていないこと(仕様として)**
- **`sw.js` の中身。** Service Worker はページのCSPが適用されないので、ここに通信コードを書けば
  持ち出せる。同一オリジンのファイルであり index.html と同じ信頼レベルなので、リポジトリへの
  書き込みを防ぐこと(＝GitHubアカウントの保護)が唯一の防御。**`sw.js` に他オリジンへの通信を足さないこと。**
- **サイトそのものの閲覧制限。** Pages はURLを知れば誰でも開ける。private リポジトリにしても
  サイトは公開されたままで、アクセス制御は GitHub Enterprise Cloud 限定。開いても中身は空なので許容している。
- **同一オリジンに置かれた別ページ。** GitHub Pages のプロジェクトページはパスで分かれていても
  オリジンは `https://yugetayuki.github.io` で共通。同じアカウントで別リポジトリを Pages 公開すると、
  そのページのスクリプトから `flourish-log-v2`(記録)と `flourish-log-v2-sync`(送信先とトークン)が
  素通しで読め、受信側の許可オリジンもオリジン単位なので直接 POST もできる。
  **このオリジンは Aubade 専用に保つこと。** 他のページを置くなら、独立したサブドメインへ移すか、
  トークンを再発行して受信側の許可オリジンを変える。
  **例外として app-hub のオリジン（`/flourish-log/` への写し）は受容した**（2026-08-21 決定）。
  同居するのは全部自作アプリで、XSS 時にトークンが漏れるリスクは app-hub 自身の
  `app-hub:sync:v1` が既に同条件のため、防御は app-hub 側の CSP に相乗りする。
  github.io 側にはこの例外を広げない。
  **同日に `script-src 'self'` を足したので、条文の射程がスクリプト実行まで広がった。**
  以前は「同一オリジンの別ページを開くと localStorage が読める」だけだったが、今は
  **同一オリジンに置かれた `/lib/appbar.js` が、Aubade を開いただけでページ権限で走る**。
  走れば `flourish-log-v2-sync`（同期トークン）に素通しで届く。したがって:
  - app-hub 側は自作ファイルしか置かない前提が、そのままこちらの前提になった
  - **github.io 側で user page リポジトリ（`yugetayuki.github.io`）を作らないこと。**
    作って `/lib/appbar.js` を置くと、旧 URL の Aubade がそれを実行する
    （2026-08-21 時点では当該リポジトリが無く 404 → catch で止まる）
  - 「読み込み先を増やさない」は**もう成り立っていない**（0 → 1 本）。増やすときは
    `script-src` を触らずに済むか、置き場が自作かを毎回見る
- **端末を他人が操作した場合。** 端末のロックが唯一の防御。
- **週報コピー→チャット貼り付け。** オーナーが能動的に外部へ出す動線であり、設計どおり。
- **tailnet の中にいる他の端末。** `*.ts.net` を許可した以上、tailnet 上の別ホストを
  送信先に設定すればそこへ送れる。防御は「オーナーしか設定タブを触らない」ことと受信側のトークン。
  tailnet に他人の端末を入れる場合はここを思い出すこと。
- **同期先PCに置かれた記録。** 送った先の保護(ディスク暗号化・OneDriveの扱い)はアプリの外。

**CSPを変更するときの確認手順**(JSDOMはCSPを解釈しないためテストでは検出できない)
1. ローカルにHTTPで配信して実ブラウザで開き、5タブすべてが描画され1タップで保存できること
2. `securitypolicyviolation` を購読した状態で外部への fetch/XHR/beacon/画像を試し、受信側に0件届くこと
3. **許可したドメインには通ること。** 緩めた側を確認しないと、締めすぎて同期が無言で止まる。
   `npm run smoke` は `*.ts.net` への fetch を横取りして到達を確認し、
   `machine.tailnet.ts.net` 形式にワイルドカードが効くことまで見る
4. Service Worker が登録され有効になること(`worker-src` を締めると無言で登録が落ち、
   オフライン起動だけが静かに壊れる)。`npm run smoke` がここまで見る

---

## 7. 経緯と「死んだ道」(再訪禁止リスト)

v2.0に至るまでに以下を検証して破棄した。同じ穴を掘り直さないこと。

1. **v1: Claude.ai アーティファクト版(React + recharts + window.storage)**
   - チャット内WebViewでは描画・操作は完動。しかし `window.storage.set` が
     「Storage set failed: Unexpected response type」で全滅。
   - 原因は公式仕様: **永続ストレージは公開(published)済みアーティファクトのみ有効**
     (Claude Help Center に明記。未公開=開発中は保存が成功しない)。
   - 公開を試行 → 「あなただけがアクセス」モードでは共有リンク自体が生成されず、
     「リンクを持つ全員」で公開してもiOSの公開ビューアで**白画面**(ErrorBoundary+バージョン表示を
     入れたv1.1でも描画された形跡なし=モジュール未実行と判断)。
   - 結論: アーティファクト独自ランタイムは本用途に対し検証不能性が高すぎる。**復帰非推奨。**
   - 当時のコード(React版)はオーナーのClaude.aiチャット履歴内に `flourish-log.jsx` として残存。
     AIレビューのプロンプト文言は v2.0 の `buildReviewText()` に移植済み。
2. **アプリ内からの Anthropic API 直呼び(キーレス)**: アーティファクト実行環境の特権であり、
   自前ホスティングのPWAでは不可。よって週次AIレビューは「コピー→チャット貼り付け」方式に変更した。
3. **代替として検討し不採用**: 貼り付け復元式の手動永続化(毎朝の摩擦が増え設計制約3に抵触)。

---

## 8. バックログ

`BACKLOG.md` を参照。着手前に `.claude/rules/10-guardrails.md` に照らすこと。

---

## 9. 報告の仕方

全プロジェクト共通の規約(事実と推測の分離など)は `.claude/rules/00-common.md` にある。
このプロジェクト固有の追加は1点だけ:

- 週報レビューを Claude 側で書くときは §2 の出力形式の契約に従い、**データにない事実を書かない**。

---

## クイックスタート

```bash
npm install
npm test
npm run smoke
```

1. `.claude/rules/10-guardrails.md` を読む(これが受け入れ基準)。
2. 変更 → テスト追加/更新 → 両方 all green。
3. リリースは `/release` スキルの手順に従う。
4. 不明点は推測で埋めず、オーナーに確認するかコードを一次情報とする。
